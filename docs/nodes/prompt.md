# MATRIX PROMPT

`MATRIX_Prompt` has one multiline `prompt` STRING widget and one STRING output with
the same name. It returns the exact value, including Unicode, empty text, spaces,
tabs, and line breaks. It performs no AI generation, provider request, template
expansion, normalization, or hidden state update.
